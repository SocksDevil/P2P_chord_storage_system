package com.feup.sdis.messages.responses;

import com.feup.sdis.messages.Status;

public class BackupResponse extends Response{
    public BackupResponse(Status status) {
        super(status);
    }
}
